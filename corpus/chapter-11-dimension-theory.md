---
type: reference
title: Chapter 11. Dimension Theory
description: '# Chapter 11. Dimension Theory'
tags:
- project
- reference
timestamp: '2026-09-11T00:00:00Z'
scope: project
source: agent
confidence: high
promotable: false
project_id: github.com__dzackgarza__lean-categories
---
# Chapter 11. Dimension Theory

# Chapter 11. Dimension Theory

| FC04 ID | Verdict | Route | Lean target(s) | Provenance | Interface comparison |
| --- | --- | --- | --- | --- | --- |
| `FC04-C11-U001` | unmatched graded finite-generation bundle | `unmatched` | partial: `GradedRing.GradeZero.isNoetherianRing`; homogeneous graded APIs | N | P proves that degree zero of a Noetherian graded ring is Noetherian and has homogeneous infrastructure, but the source bundle also requires finite homogeneous generation of the whole Noetherian graded ring over degree zero and homogeneous finite generators/component finiteness for every finite graded module. The missing graded Hilbert-basis converse is exactly the C10-U029 gap. |
| `FC04-C11-U002` | exact Poincaré-series construction from native power series | `mathlib` | `Mathlib/Algebra/PowerSeries/Basic.lean::PowerSeries.mk` | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | The source definition is literally the power series whose coefficient in degree `n` is `λ(M_n)`. `PowerSeries.mk` constructs exactly that series from the coefficient function. Additivity of `λ` is a hypothesis used by Hilbert–Serre, not extra data in the definition; the project `PoincareSeries` is only this one-line specialization. |
| `FC04-C11-U003` | unmatched Hilbert–Serre theorem | `unmatched` | partial: `Mathlib/RingTheory/Polynomial/HilbertPoly.lean` rational-series coefficient backend | N | P/H can manipulate a series already known to have denominator `(1-X)^d`, but contain no theorem deriving the Hilbert–Serre rational form from a finitely generated graded module. Current FLT explicitly records this as still missing. |
| `FC04-C11-U004` | exact pole-order notation once Hilbert–Serre supplies rationality | `mathlib` | `Mathlib/FieldTheory/RatFunc/Basic.lean::RatFunc.denom`; `Mathlib/Algebra/Polynomial/Div.lean::Polynomial.rootMultiplicity`; U002 `PowerSeries.mk`; source U003 Hilbert–Serre rationality | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | U003 is the separate theorem asserting that the Poincaré series is a rational function. For that rational function, the pole order at `t=1` is the multiplicity of `1` in its reduced denominator, exactly `RatFunc.denom.rootMultiplicity 1`. Thus `d(M)` is source notation assembled from native rational-function/polynomial data; it is not an unmatched construction merely because its prerequisite rationality theorem must first be proved. |
| `FC04-C11-U005` | unmatched eventual Hilbert polynomial theorem | `unmatched` | partial: `Polynomial.coeff_mul_invOneSubPow_eq_hilbertPoly_eval` | N | P proves eventual polynomial coefficients for an already supplied rational function `p/(1-X)^d`, but without U003 there is no theorem producing that rational form from the graded module and no source-shaped degree `d(M)-1` conclusion. |
| `FC04-C11-U006` | unmatched integer-valued polynomial example | `unmatched` | generic rational/polynomial arithmetic only | N | The elementary example `x(x+1)/2` is readily expressible in Lean, but no checked project/P/H/current external declaration owns the source example as a reusable formalized unit. |
| `FC04-C11-U007` | exact eventual-polynomial notation from native filters and polynomials | `mathlib` | `Mathlib/Order/Filter/AtTopBot/CountablyGenerated.lean::Filter.atTop`; `Mathlib/Algebra/Polynomial/Eval/Defs.lean::Polynomial.eval`; `Mathlib/Algebra/Polynomial/Roots.lean::Polynomial.eq_of_infinite_eval_eq`; source U005 eventual-polynomial existence | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | Corollary 11.2 (U005) separately supplies a rational polynomial agreeing with `n ↦ λ(M_n)` for all sufficiently large `n`. “Eventually” is `∀ᶠ n in atTop`, evaluation is `Polynomial.eval`, and equality on the infinite tail makes the polynomial unique. The row only names that already-supplied unique polynomial; the project `IsEventuallyPolynomial`/`eventualPolynomial` wrappers are not independent owners. |
| `FC04-C11-U008` | exact module non-zero-divisor definition | `mathlib` | `Mathlib/RingTheory/Regular/IsSMulRegular.lean::IsSMulRegular`; `isSMulRegular_iff_right_eq_zero_of_smul` | P | `IsSMulRegular M x` is exactly injectivity of scalar multiplication by `x`, equivalently `x • m = 0 → m = 0`. |
| `FC04-C11-U009` | unmatched Hilbert-dimension drop | `unmatched` | partial: `IsSMulRegular`; exact-sequence/graded APIs | N | P/H have the non-zero-divisor and quotient module ingredients, but the conclusion is phrased in the missing pole-order invariant `d(M)`, so no exact owner exists. |
| `FC04-C11-U010` | exact additive length convention | `mathlib` | `Mathlib/RingTheory/Length.lean::Module.length`; `Module.length_ne_top`; `Module.length_eq_add_of_exact` | P | Module length is finite under the Artinian/Noetherian hypotheses and is additive in short exact sequences, exactly the source specialization of `λ` to length over an Artin base. |
| `FC04-C11-U011` | unmatched polynomial-ring Poincaré example | `unmatched` | partial: monomial bases; project `PoincareSeries`; power-series identities | N | P/H have the degree-monomial combinatorics and formal series algebra, but no checked whole-row theorem identifies the length Poincaré series of the standard polynomial ring with `(1-t)^{-s}`. |
| `FC04-C11-U012` | unmatched Hilbert–Samuel stable-filtration theorem | `unmatched` | partial: stable filtrations, module length, `Polynomial.hilbertPoly` backend | N | No project/P/H/current external theorem proves eventual polynomiality of `length(M/M_n)` for stable primary filtrations together with the generator bound and filtration-independence of degree/leading coefficient. Imperial FLT intentionally replaces this polynomial theorem by growth order. |
| `FC04-C11-U013` | exact Hilbert–Samuel characteristic-polynomial notation from length and quotient primitives | `mathlib` | `Mathlib/RingTheory/Length.lean::Module.length`; `Ideal.pow`; `Submodule.Quotient`; `Mathlib/Algebra/Polynomial/Eval/Defs.lean::Polynomial.eval`; source U012 eventual polynomiality | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | Proposition 11.4 (U012) is the separate theorem proving finite length and eventual polynomiality. The sequence itself is the native module length of `M/(q^n M)`, and `χ_q^M` is the unique polynomial supplied by U012 that agrees with this sequence eventually; `χ_q` is the specialization `M=A`. The project `HilbertSamuelFunction` and `CharacteristicPolynomial` merely package these primitives. |
| `FC04-C11-U014` | unmatched Hilbert–Samuel corollary | `unmatched` | partial: U012 ingredients | N | P/H do not prove that `length(A/q^n)` eventually agrees with a polynomial of degree bounded by the least number of generators of `q`. |
| `FC04-C11-U015` | unmatched primary-ideal degree invariance | `unmatched` | partial: primary ideals, cofinal adic filtrations | N | Cofinality and primary-ideal infrastructure exist, but the source conclusion compares degrees of Hilbert–Samuel characteristic polynomials, which are not formalized. |
| `FC04-C11-U016` | exact local Hilbert–Samuel dimension notation from polynomial degree | `mathlib` | `Mathlib/Algebra/Polynomial/Degree/Defs.lean::Polynomial.natDegree`; `IsLocalRing.maximalIdeal`; U013 characteristic-polynomial data; source U015 common-degree theorem | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | Proposition 11.6 (U015) separately proves that all `m`-primary characteristic polynomials have the same degree. The invariant `d(A)` is therefore that common `Polynomial.natDegree`, and may be read from `χ_m`. The comparison with the associated graded ring is theorem/comparison content built from the already-defined graded invariant U004; it does not make this notation an unmatched object. |
| `FC04-C11-U017` | exact least-generator invariant from native ideals and natural infimum | `mathlib` | `Ideal.span`; `Ideal.radical`; `IsLocalRing.maximalIdeal`; `Nat.sInf`, `Nat.sInf_mem`, `Nat.sInf_le`; Noetherian finite-generation API | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | `δ(A)` is the infimum in `ℕ` of cardinalities of finite families whose generated ideal has radical `m`. Noetherianity gives such a finite generating family, and `Nat.sInf_mem`/`Nat.sInf_le` provide attainment and minimality. The equality `δ(A)=d(A)=dim A` is the downstream Theorem 11.14 (U027), not part of this definition; the project `DeltaInvariant` only spells out this native infimum. |
| `FC04-C11-U018` | unmatched inequality δ ≥ d | `unmatched` | partial external: FLT growth-degree inequality | N | FLT proves the analogous inequality for its growth-order replacement, not the source Hilbert–Samuel polynomial degree `d(A)`. P/H have no exact theorem. |
| `FC04-C11-U019` | unmatched Hilbert–Samuel quotient inequality | `unmatched` | partial: Artin–Rees; `IsSMulRegular`; length | N | The algebraic ingredients exist, but the degree comparison `deg χ_q^{M/xM} ≤ deg χ_q^M - 1` depends on absent Hilbert–Samuel polynomial objects. |
| `FC04-C11-U020` | unmatched local d quotient inequality | `unmatched` | partial: P dimension drop theorem; U019 ingredients | N | P has Krull-dimension drop for a regular element, but not the source Hilbert–Samuel-degree invariant `d(A)`, so it does not satisfy this row. |
| `FC04-C11-U021` | unmatched d ≥ Krull dimension | `unmatched` | partial: P Krull dimension; external FLT growth-degree equality | N | Current exact developments compare Krull dimension with growth order, not with the source degree of `χ_m`; the stated inequality is therefore not represented. |
| `FC04-C11-U022` | exact finite-dimensionality of Noetherian local rings | `mathlib` | `Mathlib/RingTheory/Ideal/KrullsHeightTheorem.lean::Ideal.finiteHeight_of_isNoetherianRing`; local `FiniteRingKrullDim` instance | P | Every ideal has finite height in a Noetherian ring; in a local ring the maximal ideal has height equal to ring Krull dimension, giving finite local dimension exactly as stated. |
| `FC04-C11-U023` | exact prime height/localization dimension comparison | `mathlib` | `Mathlib/RingTheory/Ideal/Height.lean::Ideal.height`; `IsLocalization.AtPrime.ringKrullDim_eq_height` | P | Mathlib's ideal height is the chain-height invariant, and localization at a prime has Krull dimension exactly that height. |
| `FC04-C11-U024` | exact finite-height/DCC assembly | `mathlib` | `Ideal.finiteHeight_of_isNoetherianRing`; `Ideal.height_strict_mono_of_isPrime_of_isPrime` | P | Every prime has finite height; strict inclusion of primes strictly raises height. Hence any descending chain below a fixed starting prime strictly decreases a finite ordinal height and terminates, yielding the source DCC consequence. |
| `FC04-C11-U025` | exact chain-theoretic prime depth from native coheight | `mathlib` | `Mathlib/Order/KrullDimension.lean::Order.coheight`, `Order.coheight_eq_krullDim_Ici`; `Mathlib/RingTheory/Spectrum/Prime/RingHom.lean::Ideal.primeSpectrumQuotientOrderIsoZeroLocus`; `Mathlib/RingTheory/KrullDimension/NonZeroDivisors.lean::ringKrullDim_quotient` | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | Atiyah–Macdonald's historical “depth” is exactly `Order.coheight p`, the supremum of lengths of prime chains beginning at `p`. Mathlib's quotient-spectrum order isomorphism identifies the interval above `p` with `Spec(A/p)`, giving the stated `depth(p)=dim(A/p)` comparison. The project `PrimeIdealDepth` is only an alias plus this standard order calculation. |
| `FC04-C11-U026` | exact system-of-parameters existence theorem | `mathlib` | `Ideal.exists_finset_card_eq_height_of_isNoetherianRing`; `IsLocalRing.maximalIdeal_height_eq_ringKrullDim`; `Ideal.sInf_minimalPrimes` | P | Applied to the maximal ideal, the pinned height theorem produces a finite generating set whose span has the maximal ideal as a minimal prime and whose cardinality is the local dimension; taking radicals gives an m-primary ideal generated by exactly `dim A` elements. The source-shaped assembly compiled against P. |
| `FC04-C11-U027` | unmatched three-way dimension theorem | `unmatched` | partial: U026 and P Krull dimension; external FLT growth theorem | N | P proves the Krull-dimension/parameter-count side, and FLT proves equality with a growth-order replacement, but no checked source proves equality with the degree of the Hilbert–Samuel polynomial `χ_m`. The three-clause source bundle remains unmatched. |
| `FC04-C11-U028` | unmatched origin-local polynomial dimension example | `unmatched` | partial: `MvPolynomial.ringKrullDim_of_isNoetherianRing`; `MvPolynomial.isRegularRing_of_isRegularRing`; `MvPolynomial.idealOfVars` | N | P has global polynomial-ring dimension, regularity, and the ideal generated by the variables, but no checked source-shaped owner/assembly was found for `dim k[x₁,…,xₙ]_(x₁,…,xₙ)=n`. |
| `FC04-C11-U029` | exact cotangent-dimension bound | `mathlib` | `ringKrullDim_le_spanFinrank_maximalIdeal`; `spanFinrank_maximalIdeal_eq_finrank_cotangentSpace` | P | The pinned height theorem gives `dim A ≤ spanFinrank(m)`, and the cotangent-space theorem identifies this generator rank with `dim_k(m/m²)`. |
| `FC04-C11-U030` | exact Krull height theorem | `mathlib` | `Mathlib/RingTheory/Ideal/KrullsHeightTheorem.lean::Ideal.height_le_card_of_mem_minimalPrimes_span_finset` | P | A prime minimal over the ideal generated by a finite set has height bounded by the cardinality of that set, exactly Corollary 11.16. |
| `FC04-C11-U031` | exact principal-ideal height-one corollary | `mathlib` | `Ideal.height_le_one_of_isPrincipal_of_mem_minimalPrimes`; `Ideal.height_span_singleton_eq_one_of_mem_nonZeroDivisors` | P | For a non-zero-divisor `x`, the principal ideal has height one, while every minimal prime over it has height at most one; the infimum characterization of ideal height forces every such minimal prime to have height exactly one. |
| `FC04-C11-U032` | exact regular-element dimension drop | `mathlib` | `Mathlib/RingTheory/KrullDimension/Regular.lean::ringKrullDim_quotient_span_singleton_succ_eq_ringKrullDim` | P | In a Noetherian local ring, quotienting by a non-zero-divisor in the maximal ideal decreases Krull dimension by exactly one, in the source's successor formulation. |
| `FC04-C11-U033` | unmatched dimension invariance under completion | `unmatched` | partial: adic completion/local ring APIs | N | Neither P nor H has a theorem identifying `ringKrullDim A` with the Krull dimension of its maximal-adic completion; current search also found no compatible external exact owner. |
| `FC04-C11-U034` | exact system-of-parameters predicate from native ideal operations | `mathlib` | `Ideal.span`; `Ideal.radical`; `IsLocalRing.maximalIdeal` | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | In the source context the family already has length `d=dim A`; being a system of parameters then means precisely that its generated ideal is `m`-primary, equivalently `radical(span(range x)) = maximalIdeal A`. All ingredients are native Mathlib ideal/local-ring notions, so the project `IsSystemOfParameters` is a convenience wrapper rather than a route. |
| `FC04-C11-U035` | unmatched coefficient theorem for parameters | `unmatched` | partial: homogeneous polynomial and parameter APIs | N | No checked project/P/H/open/external declaration proves that a homogeneous relation modulo `q^{s+1}` for a system of parameters forces all coefficients into the maximal ideal. |
| `FC04-C11-U036` | unmatched algebraic-independence corollary | `unmatched` | partial: `AlgebraicIndependent`; project `IsSystemOfParameters` | N | P/H have algebraic-independence APIs, but no theorem derives independence of a system of parameters from a coefficient field via Proposition 11.20. |
| `FC04-C11-U037` | exact regular-local definition | `mathlib` | `Mathlib/RingTheory/RegularLocalRing/Defs.lean::IsRegularLocalRing`; `isRegularLocalRing_iff` | P | Mathlib defines a Noetherian local ring to be regular exactly when the generator rank of its maximal ideal equals its Krull dimension, one of the source's equivalent regularity conditions. |
| `FC04-C11-U038` | unmatched three-way regularity theorem | `unmatched` | partial: `IsRegularLocalRing.iff_finrank_cotangentSpace`; C10 associated-graded PRs | N | P gives equivalence between regularity, cotangent-space dimension, and maximal-ideal generator rank, but not the source's additional isomorphism `G_m(A) ≃ k[t₁,…,t_d]`. Associated graded structures are themselves only open-PR ports, and no checked theorem supplies this polynomial identification. |
| `FC04-C11-U039` | unmatched domain lifting from associated graded | `unmatched` | partial: separated adic/Krull intersection; associated-graded PRs | N | No checked P/H/open/external theorem states that a separated filtered ring with integral-domain associated graded ring is itself a domain. |
| `FC04-C11-U040` | unmatched regular-dimension-one/DVR equivalence | `unmatched` | partial: `IsRegularLocalRing` instance for local principal ideal domains; DVR APIs | N | P proves the DVR/local-PID direction gives a regular local ring, but no checked converse derives a DVR from one-dimensional regular-local hypotheses. Strict biconditional semantics make the row unmatched. |
| `FC04-C11-U041` | unmatched integral-closure regularity bundle | `unmatched` | partial: integral-closure APIs; regular-local definitions | N | No checked owner proves the source general theorem that integrally closed associated graded implies the local ring integrally closed, nor the resulting regular-local implication together with the higher-dimensional converse failure. |
| `FC04-C11-U042` | unmatched regularity under completion | `unmatched` | partial: adic completion; regular-local APIs | N | P/H have no theorem `A regular ↔ Â regular`; current Mathlib search and external indexed search found no exact compatible owner. |
| `FC04-C11-U043` | unmatched analytic irreducibility consequence | `unmatched` | partial: completion/domain terminology | N | Since regularity of completion and its domain consequence are not formalized in the needed generality, no checked row owner proves the source analytic-irreducibility comparison. |
| `FC04-C11-U044` | unmatched coefficient-field power-series completion theorem | `unmatched` | partial: formal power series and adic completion equivalences | N | P/H can identify the completion of a polynomial ring at the variable ideal with formal power series, but no Cohen-style theorem identifies the completion of an arbitrary equicharacteristic regular local ring with `k[[t₁,…,t_d]]`. |
| `FC04-C11-U045` | exact affine-space regular-local example | `mathlib` | `Mathlib/RingTheory/RegularLocalRing/Polynomial.lean::MvPolynomial.isRegularRing_of_isRegularRing`; `IsRegularRing.isRegularLocalRing_localization` | P | A polynomial ring in finitely many variables over a field is regular, and localization of a regular ring at every prime is regular local; specializing to the origin maximal ideal gives the source example. |
| `FC04-C11-U046` | exact affine function-field definition | `mathlib` | `Mathlib/AlgebraicGeometry/FunctionField.lean::Scheme.functionField`; `functionField_isFractionRing_of_affine` | P | For an integral affine scheme, Mathlib's function field is a field and is a fraction ring of the affine coordinate ring, exactly the source rational-function field construction. |
| `FC04-C11-U047` | exact affine transcendence-dimension formula from native coordinate/fraction-field primitives | `mathlib` | `MvPolynomial`; `Ideal.Quotient`; `Mathlib/RingTheory/Localization/FractionRing.lean::FractionRing`; `Mathlib/RingTheory/AlgebraicIndependent/Basic.lean::Algebra.trdeg` | P=db584cd6d46c92f209a44c0f1c829460d327499d; clause-wise remap 2026-09-13 | For an irreducible affine variety with prime ideal `p`, its coordinate domain is the native quotient `k[x]/p`, its rational-function field is the native fraction ring, and the source dimension is exactly `Algebra.trdeg k` of that field. The project `AffineVarietyDimension` is just this formula packaged around a `PrimeSpectrum` point. |
| `FC04-C11-U048` | exact Nullstellensatz point/maximal-ideal assembly | `mathlib` | `Mathlib/RingTheory/Nullstellensatz.lean::MvPolynomial.isMaximal_iff_eq_vanishingIdeal_singleton`; `mem_zeroLocus_iff`; quotient ideal correspondence | P | Over an algebraically closed field every maximal ideal of the polynomial ring is the vanishing ideal of a unique point in the finite-variable setting; restricting to maximal ideals containing the defining prime is equivalent to the point lying in its zero locus, and quotient ideal correspondence gives maximal ideals of the coordinate ring. |
| `FC04-C11-U049` | exact local-dimension definition/comparison | `mathlib` | `Mathlib/AlgebraicGeometry/Properties.lean::ringKrullDim_stalk_eq_coheight`; `IsLocalization.AtPrime.ringKrullDim_eq_height` | P | The local ring at an affine-scheme point is the localization at its prime ideal, and its Krull dimension is the point's coheight/ideal height. For a closed point this is exactly the source local dimension `dim A(V)_m`. |
| `FC04-C11-U050` | unmatched affine local/global transcendence-dimension theorem | `unmatched` | partial: project `AffineVarietyDimension`; P local dimension | N | No checked P/H/current external theorem identifies every maximal-local Krull dimension of a finite-type integral affine algebra over an algebraically closed field with the transcendence degree of its fraction field. |
| `FC04-C11-U051` | unmatched transcendence/local-dimension inequality | `unmatched` | partial: project trdeg definition; P local Krull dimension | N | Current searches found no theorem proving the source inequality `trdeg_k k(V) ≥ dim A(V)_m`; the two notions exist but are not connected by a checked declaration. |
| `FC04-C11-U052` | unmatched integral-extension local-dimension equality | `unmatched` | partial: `Ideal.isMaximal_comap_of_isIntegral_of_isMaximal`; `IntegralClosure/GoingDown.lean`; `Ideal.exists_ltSeries_of_hasGoingDown`; localization height theorem | N | P proves contraction of maximal ideals and going down for integral extensions over integrally closed domains, but no checked source-shaped theorem/assembly was found proving equality of the two localized Krull dimensions under exactly the source hypotheses without extra Noetherian assumptions. |
| `FC04-C11-U053` | unmatched maximal-local/global affine dimension corollary | `unmatched` | partial: U047/U049 infrastructure | N | This corollary is the same missing transcendence-degree versus local Krull-dimension bridge as U050; no exact owner exists at P/H or in the checked current external sources. |


