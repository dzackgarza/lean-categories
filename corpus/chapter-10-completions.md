---
type: reference
title: Chapter 10. Completions
description: '# Chapter 10. Completions'
tags:
- project
- reference
timestamp: '2026-09-11T00:00:00Z'
scope: project
source: agent
confidence: high
promotable: false
project_id: github.com__dzackgarza__lean-categories
---
# Chapter 10. Completions

# Chapter 10. Completions

| FC04 ID | Verdict | Route | Lean target(s) | Provenance | Interface comparison |
| --- | --- | --- | --- | --- | --- |
| `FC04-C10-U001` | exact topological-group bundle | `mathlib` | `Mathlib/Topology/Algebra/Group/Basic.lean::IsTopologicalAddGroup`; `Homeomorph.addLeft`; `map_add_left_nhds_zero`; `Mathlib/Topology/Algebra/Group/Pointwise.lean::IsTopologicalAddGroup.t2Space_iff_zero_closed` | P | Addition/inversion continuity is the bundled topological-add-group structure. Translation is a homeomorphism, zero-neighborhoods determine all neighborhoods, and in a topological additive group closed `{0}` is equivalent to Hausdorffness. |
| `FC04-C10-U002` | exact separation subgroup/quotient assembly | `mathlib` | `Mathlib/Topology/Separation/Basic.lean::R0Space.closure_singleton`; `Mathlib/Topology/Algebra/FilterBasis.lean::AddGroupFilterBasis.t2Space_iff`; `Mathlib/Topology/Algebra/Group/Quotient.lean::QuotientAddGroup.instT3Space` | P | In a topological additive group the neighborhood-kernel at zero is the closure of `{0}` and is an additive subgroup. Quotienting by that closed subgroup is Hausdorff; the filter-basis criterion identifies Hausdorffness with trivial intersection. |
| `FC04-C10-U003` | exact equivalent completion construction | `mathlib` | `Mathlib/Topology/UniformSpace/Completion.lean::UniformSpace.Completion`; `Completion.denseRange_coe`; `Mathlib/Topology/Algebra/GroupCompletion.lean::UniformSpace.Completion.toCompl`; `Mathlib/Topology/UniformSpace/Cauchy.lean::CauchySeq` | P | Mathlib constructs the same Hausdorff completion by Cauchy filters modulo inseparability, without the source's countable-basis restriction. `CauchySeq` supplies the sequence formulation when desired, and `toCompl` is the canonical additive homomorphism. |
| `FC04-C10-U004` | exact kernel/separation comparison | `mathlib` | `UniformSpace.Completion.isUniformInducing_coe`; `SeparationQuotient.mk_eq_mk`; `R0Space.closure_singleton`; `UniformSpace.Completion.coe_injective`; `IsTopologicalAddGroup.t2Space_iff_zero_closed` | P | Equality in the separated completion is exactly source inseparability pulled back along the uniform inducing map. For a topological additive group the inseparability class of zero is `closure {0}`, i.e. the intersection of its neighborhoods; therefore the completion map is injective exactly in the Hausdorff case. |
| `FC04-C10-U005` | exact functorial completion | `mathlib` | `Mathlib/Topology/Algebra/GroupCompletion.lean::AddMonoidHom.completion`; `AddMonoidHom.continuous_completion`; `Mathlib/Topology/UniformSpace/Completion.lean::UniformSpace.Completion.map_comp` | P | A continuous additive homomorphism is uniformly continuous for the canonical group uniformities, extends to completions, and the underlying completion maps satisfy the source composition law. |
| `FC04-C10-U006` | complete subgroup-basis topology construction | `project-existing` | `LeanCategories/ForMathlib/AddSubgroupTopology.lean::AddSubgroup.filterBasisOfAntitone`; `AddSubgroup.topologyOfAntitone`; `AddSubgroup.hasBasis_nhds_zero_topologyOfAntitone`; `AddSubgroup.isOpen_topologyOfAntitone`; `AddSubgroup.isClosed_topologyOfAntitone`; P `Ideal.hasBasis_nhds_zero_adic` | Project `df31fa47440c04b88db5f03af6cce3cd2beaca67`; P | The project constructs the topology from an arbitrary descending additive-subgroup sequence and proves exactly that the defining subgroups form the zero-neighborhood basis and are open and closed. The source specialization `p^n Z` is identified in `FiltrationTopology.lean` with the principal-ideal power filtration. No Hausdorff hypothesis is imposed. |
| `FC04-C10-U007` | complete coherent-residue completion comparison | `project-existing` | `LeanCategories/ForMathlib/AddSubgroupCompletion.lean::AddSubgroup.Completion`; `AddSubgroup.Completion.of`; `AddSubgroup.Completion.eval`; `AddSubgroup.Completion.tendsto_out`; `AddSubgroup.Completion.cauchySeq_out`; `AddSubgroup.Completion.denseRange_of`; `AddSubgroup.Completion.completionAddEquiv`; `AddSubgroup.Completion.completionAddEquiv_coe` | Project `3ae9d57fb4ed76086d09f4c9195aa5af9f832d70`; P | For an arbitrary descending subgroup family, the project defines the coherent residue families `x_n ∈ G/F_n`, constructs the diagonal map, proves representatives of every coherent family form a Cauchy sequence, and proves their diagonal images converge to that family. The coherent-residue group is complete and canonically topologically additively equivalent to Mathlib's uniform completion of the subgroup-basis topology, extending the original map. This supplies both directions of the source comparison without a Hausdorff assumption on `G`. |
| `FC04-C10-U008` | complete quotient inverse-system construction and limit | `project-existing` | `LeanCategories/ForMathlib/AddSubgroupCompletion.lean::AddSubgroup.quotientTransition`; `AddSubgroup.quotientInverseSystem`; `AddSubgroup.completionSubgroup`; `AddSubgroup.Completion`; `AddSubgroup.Completion.eval`; P `Mathlib/Order/DirectedInverseSystem.lean::InverseSystem` | Project `3ae9d57fb4ed76086d09f4c9195aa5af9f832d70`; P | The transition maps `G/F_n → G/F_m` for `m≤n` satisfy identity and composition, giving Mathlib's inverse-system structure. `completionSubgroup` is exactly the subgroup of the product satisfying all transition coherences, hence the source inverse limit for this quotient system; U007 identifies it canonically with the topological completion. |
| `FC04-C10-U009` | complete cofinality/topology comparison | `project-existing` | `LeanCategories/ForMathlib/AddSubgroupTopology.lean::AddSubgroup.topologyOfAntitone_eq_iff`; `AddSubgroup.continuous_topologyOfAntitone_iff`; P `UniformSpace.Completion` | Project `df31fa47440c04b88db5f03af6cce3cd2beaca67`; P | Two descending subgroup families generate the same topology exactly when they are mutually cofinal. Since a topological additive group's canonical uniformity, and hence its completion, is determined by that topology, this supplies the source choice-independence comparison while keeping the chosen inverse-system presentation distinct from the topological completion. |
| `FC04-C10-U010` | exact inverse-system surjectivity notion | `mathlib` | `Mathlib/Order/DirectedInverseSystem.lean::InverseSystem`; `Function.Surjective` | P | Mathlib's transition-law structure together with pointwise `Function.Surjective` is exactly the source notion that every transition homomorphism is surjective. |
| `FC04-C10-U011` | exact categorical exact-system construction | `mathlib` | `Mathlib/Algebra/Homology/ShortComplex/FunctorEquivalence.lean::ShortComplex.functorEquivalence`; functor-category abelian/exactness API; `CategoryTheory.Limits.lim.map` | P | A compatible family of short exact sequences is equivalently a functor into short complexes, with exactness checked pointwise. A natural transformation induces the source maps on inverse limits through `lim.map`. |
| `FC04-C10-U012` | revision-specific external exact inverse-limit theorem | `reference-port` | `LiranShaul/lean-faithfully-flat-descent@f5967aab9067290aed1b7113569c1b9024a9fca4::lib/mlSystem.lean::InverseSystem.surjective_limit_of_mittagLeffler_exact`; `surjective_is_mittag_leffler` | E | The external theorem proves right exactness on inverse limits under the Mittag-Leffler condition; its surjective-transition lemma specializes exactly to Proposition 10.2. Apache-2.0; source Lean `v4.26.0-rc2`, Mathlib `d5c9558e75342a10d6321e6a8c798a14f68ae23c`. The file compiled unchanged against P. |
| `FC04-C10-U013` | exact sequential derived-obstruction definition | `project-existing` | `LeanCategories/ForMathlib/SequentialInverseLimitOne.lean::SequentialInverseSystem.difference`; `SequentialInverseSystem.mem_ker_difference_iff`; `SequentialInverseSystem.limOne` | Project `d46b2961c14d4ee7545c6c7e00da549c2ce8f1b5` | The project defines the source difference map on `∏ A_n` by `a_n - θ_n(a_(n+1))`, proves its kernel is exactly the coherent inverse-limit families, and defines `lim¹ A_n` as the quotient by its range, i.e. `Coker d^A`. This matches the printed p. 104 remark rather than the garbled frozen OCR. |
| `FC04-C10-U014` | unmatched exactness of subgroup-topology completion | `unmatched` | partial: short exact sequences; completion; U012 external theorem | N | The abstract ingredients exist, but no checked owner assembles the induced subgroup/quotient topologies, identifies their completions with the corresponding residue inverse limits, and proves the displayed completed short exact sequence. |
| `FC04-C10-U015` | unmatched completed-subgroup quotient theorem | `unmatched` | partial: completion; quotient groups | N | No checked P/H/open/external theorem identifies the completion of each basis subgroup `G_n` with a subgroup of `Ĝ` and proves `Ĝ/Ĝ_n ≃ G/G_n` as a whole bundle. |
| `FC04-C10-U016` | exact idempotence/completeness theorem | `mathlib` | `Mathlib/Topology/UniformSpace/Completion.lean::UniformCompletion.completeEquivSelf`; `UniformSpace.Completion.instCompleteSpace` | P | Completion is complete and separated; applying `completeEquivSelf` to an already complete separated completion gives the canonical idempotence `Completion (Completion G) ≃ Completion G`. |
| `FC04-C10-U017` | exact equivalent completeness convention | `mathlib` | `CompleteSpace`; `T0Space`; `UniformCompletion.completeEquivSelf`; `UniformSpace.Completion.coe_injective` | P | The source's “canonical completion map is an isomorphism” is Mathlib's `CompleteSpace` together with separation (`T0Space`, equivalent to Hausdorff for topological groups). Under those hypotheses `completeEquivSelf` is the canonical uniform equivalence, so source completeness includes Hausdorffness exactly as stated. |
| `FC04-C10-U018` | complete project-owned adic ring topology/completion bundle | `project-existing` | `LeanCategories/ForMathlib/AdicCompletionTopology.lean::AdicCompletion.adicCompletionTopologicalSpace`; `AdicCompletion.instIsTopologicalRing`; `AdicCompletion.continuous_eval`; `AdicCompletion.continuous_of`; `AdicCompletion.denseRange_of`; `LeanCategories/ForMathlib/AdicCompletionUniform.lean::AdicCompletion.instCompleteSpace`; `AdicCompletion.completionAddEquiv`; P `Ideal.hasBasis_nhds_zero_adic`; `AdicCompletion.of_injective_iff` | Projects `12366c338a380f59cc25cdee35f8023e322d88f5`, `df31fa47440c04b88db5f03af6cce3cd2beaca67`; P | The ideal-power topology on the ring, the inverse-limit topology on its coherent residue completion, continuity and density of the canonical map, completeness, and the comparison with Mathlib's abstract uniform completion realize the source topological ring completion. The kernel/injectivity criterion remains the pinned algebraic result. |
| `FC04-C10-U019` | complete topological module-completion bundle | `project-existing` | `LeanCategories/ForMathlib/AdicModuleTopology.lean::Ideal.hasBasis_nhds_zero_adicModule`; `Ideal.continuous_linearMap_adic`; `LeanCategories/ForMathlib/AdicCompletionTopology.lean::AdicCompletion.adicCompletionTopologicalSpace`; `AdicCompletion.instContinuousSMul`; `AdicCompletion.continuous_map`; `AdicCompletion.continuous_of`; `AdicCompletion.denseRange_of`; `LeanCategories/ForMathlib/AdicCompletionUniform.lean::AdicCompletion.instCompleteSpace`; `AdicCompletion.completionAddEquiv` | Projects `12366c338a380f59cc25cdee35f8023e322d88f5`, `df31fa47440c04b88db5f03af6cce3cd2beaca67`; P | For every module, `I^n M` is the zero-neighborhood basis; all linear maps are continuous and induce continuous maps on completions. The completed ring acts jointly continuously on the completed module. The coherent-residue completion is complete and canonically topologically additively equivalent to the abstract uniform completion, with no finite-generation or separatedness assumption on the original module. |
| `FC04-C10-U020` | exact formal-power-series completion | `mathlib` | `Mathlib/RingTheory/MvPowerSeries/Equiv.lean::MvPowerSeries.toAdicCompletionAlgEquiv` | P | Specializing the finite-variable theorem to one variable identifies the `(x)`-adic completion of `k[x]` with `k[[x]]`, exactly the source example. |
| `FC04-C10-U021` | unmatched bundled p-adic example | `unmatched` | partial: `PadicInt`; `PadicInt.toZModPow`; `PadicInt.ker_toZModPow`; `PadicInt.adicCompletionIntegersEquiv`; adic convergence APIs | N | P/H formalize `ℤ_p`, its compatible residues, completeness, and adic-completion comparisons, but no checked declaration gives the source digit expansion `∑ a_n p^n` with `0≤a_n≤p-1` as part of the same bundle. Current external searches (including `rwst/lean-code@ce1619b6418dc263efb911b11ad09409c2083d80`, `YijunYuan/FormalizedSparse@be9fb43dded873bc7567b486e17e924823e10e41`, and `CBirkbeck/AINTLIB@160e446617a2168c34c95bbe7a76c4105b392434`) found no complete owner. |
| `FC04-C10-U022` | exact broader filtration structure plus source normalization | `mathlib` | P `Mathlib/RingTheory/Filtration.lean::Ideal.Filtration`; `Ideal.Filtration.Stable`; `Ideal.stableFiltration`; `Ideal.stableFiltration_stable`; project `LeanCategories/ForMathlib/FiltrationTopology.lean::Ideal.Filtration.Stable.topology_eq_adic` | P; Project `df31fa47440c04b88db5f03af6cce3cd2beaca67` | Mathlib's filtration is deliberately more general: it does not require the source condition `F.N 0 = ⊤`. The source notion is therefore a Mathlib filtration together with that equality. `Stable` is eventual equality, and the power filtration is stable. The project theorem uses the normalization explicitly; the zero-filtration separating example proves it cannot be omitted. |
| `FC04-C10-U023` | complete stable-filtration topology comparison | `project-existing` | `LeanCategories/ForMathlib/FiltrationTopology.lean::Ideal.Filtration.topology`; `Ideal.Filtration.Stable.topology_eq`; `Ideal.Filtration.topology_stableFiltration_top`; `Ideal.Filtration.Stable.topology_eq_adic`; P `Ideal.Filtration.Stable.bounded_difference` | Project `df31fa47440c04b88db5f03af6cce3cd2beaca67`; P | The project turns any filtration into its subgroup-basis topology. Mathlib's bounded-difference theorem yields equality of topologies for stable filtrations with the same initial term; with the source normalization `F.N 0 = ⊤`, every stable `I`-filtration induces exactly the `I`-adic topology. The constant-zero/constant-whole stable filtrations over `I=⊤` separate stability from the missing normalization. |
| `FC04-C10-U024` | exact graded-ring structure | `mathlib` | `Mathlib/RingTheory/GradedAlgebra/Basic.lean::GradedRing`; `DirectSum.Decomposition`; `GradedRing.proj` | P | The internal graded-ring class records multiplication of grades and a direct-sum decomposition; grade zero inherits ring structure and each grade the expected module structure. |
| `FC04-C10-U025` | exact polynomial grading | `mathlib` | `Mathlib/RingTheory/MvPolynomial/Homogeneous.lean::MvPolynomial.homogeneousSubmodule`; `MvPolynomial.gradedAlgebra`; `homogeneousComponent` | P | The homogeneous degree-`n` polynomial submodules form the standard grading of a polynomial algebra, specializing to the source finite polynomial ring. |
| `FC04-C10-U026` | exact graded-module/decomposition interfaces | `mathlib` | `Mathlib/Algebra/Module/GradedModule.lean::SetLike.GradedSMul`; `DirectSum.Decomposition`; `DirectSum.decompose`; `DirectSum.sum_support_decompose`; homogeneous-element API | P | Mathlib's internal graded-module interface is a direct-sum decomposition compatible with graded scalar multiplication. `decompose` has finite support and reconstructs each element uniquely as the sum of homogeneous components. |
| `FC04-C10-U027` | exact graded homomorphism interface | `mathlib` | `Mathlib/RingTheory/GradedAlgebra/RingHom.lean::GradedRingHom`; graded linear-map/`map_mem` API | P | The graded-map structures require the underlying linear/additive map to send each degree component into the same prescribed degree, exactly the source condition. |
| `FC04-C10-U028` | exact positive-degree ideal | `mathlib` | `Mathlib/RingTheory/GradedAlgebra/Homogeneous/Ideal.lean::HomogeneousIdeal.irrelevant`; `mem_irrelevant_iff`; `irrelevant_eq_iSup` | P | The irrelevant ideal is precisely the ideal generated by all nonzero/positive homogeneous degrees; over `ℕ` this is the source `A_+ = ⊕_{n>0} A_n`. |
| `FC04-C10-U029` | unmatched graded Noetherian biconditional | `unmatched` | partial: `Mathlib/RingTheory/GradedAlgebra/Noetherian.lean::GradedRing.GradeZero.isNoetherianRing`; generic `Algebra.FiniteType.isNoetherianRing`; `GradedAlgebra.exists_finset_adjoin_eq_top_and_homogeneous` | N | P/H prove `A` Noetherian implies `A_0` Noetherian and prove the forward Hilbert-basis direction from finite type over a Noetherian base, but no checked theorem derives finite generation of an arbitrary `ℕ`-graded Noetherian ring over `A_0`. `tom111/BEI-lean@5b3ddff1ffac4a12d16da43df6fc443501f4d680` only assumes both hypotheses. |
| `FC04-C10-U030` | complete graded Rees ring/module construction | `project-existing` | P `Mathlib/Algebra/DirectSum/Internal.lean::Submodule.nat_power_gradedMonoid`; `SetLike.gsemiring`; `Mathlib/RingTheory/Filtration.lean::Ideal.Filtration.pow_smul_le`; project `LeanCategories/ForMathlib/FiltrationGraded.lean::Ideal.Filtration.gradedSMulPowers`; P `SetLike.gmodule`; `Gmodule.module`; `Ideal.Filtration.submodule` | Project `2ae5f91aeee7c9d370631213d0359ee098154f01`; P | Mathlib already gives the graded multiplicative structure on the degree pieces `I^n` and the external direct-sum ring. The project supplies exactly the missing homogeneous action `I^i • F_j ≤ F_(i+j)` using `F.pow_smul_le`; Mathlib then builds the graded module `⊕ F_n` over `⊕ I^n`. No stability hypothesis is imposed, matching the source construction; stability enters only in Lemma 10.8. |
| `FC04-C10-U031` | exact Rees-algebra Noetherianity | `mathlib` | `Mathlib/RingTheory/ReesAlgebra.lean::reesAlgebra`; `adjoin_monomial_eq_reesAlgebra`; `reesAlgebra.fg`; Noetherian instance | P | For a finitely generated ideal the Rees algebra is generated in degree one; over a Noetherian ring the ideal is finitely generated and the Rees algebra is Noetherian, exactly the source comparison. |
| `FC04-C10-U032` | exact Lemma 10.8 | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.Filtration.submodule_fg_iff_stable` | P | For finitely generated filtration pieces, finite generation of the filtration's Rees submodule is equivalent to stability, exactly Lemma 10.8. |
| `FC04-C10-U033` | exact Artin-Rees stability theorem | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.Filtration.Stable.inter_right`; `Stable.of_le` | P | Intersecting a stable filtration with a submodule in a finite module over a Noetherian ring remains stable, exactly Proposition 10.9. |
| `FC04-C10-U034` | exact Corollary 10.10 | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.exists_pow_inf_eq_pow_smul` | P | This is the Artin-Rees formula `(I^n M) ∩ M' = I^(n-k)((I^k M) ∩ M')` after the theorem's index normalization. |
| `FC04-C10-U035` | exact strong Artin-Rees bounded difference | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.Filtration.Stable.bounded_difference`; `Stable.inter_right`; stable power filtration | P | Applying bounded difference to the intrinsic power filtration on `M'` and the induced intersection filtration gives exactly the source bounded-difference assertion. The source-shaped assembly was compile-checked against P; equality of the induced adic topologies follows from the same two-sided cofinal bounds. |
| `FC04-C10-U036` | unmatched editorial comparison | `unmatched` | partial: U035 strong Artin-Rees theorem | N | The mathematical strong theorem is formalized, but the source row is specifically the named distinction and later-use comparison between the strong assertion and its topological consequence; no formal declaration owns that editorial comparison as a source-shaped unit. |
| `FC04-C10-U037` | exact completion exactness theorem | `mathlib` | `Mathlib/RingTheory/AdicCompletion/Exactness.lean::AdicCompletion.map_injective`; `map_exact`; `map_surjective` | P | These three theorems assemble the completed short exact sequence for finite modules over a Noetherian ring. The complete source-shaped sequence was compile-checked against P. |
| `FC04-C10-U038` | exact natural tensor-completion map | `mathlib` | `Mathlib/RingTheory/AdicCompletion/AsTensorProduct.lean::AdicCompletion.ofTensorProduct`; `ofTensorProduct_tmul`; `ofTensorProduct_naturality` | P | `ofTensorProduct` is exactly the canonical `Â ⊗_A M → M̂` comparison and is natural in `M`. |
| `FC04-C10-U039` | unmatched failure counterexamples | `unmatched` | partial: `AdicCompletion.ofTensorProduct`; finite-generation recovery theorems | N | P/H/open/external search found no explicit examples witnessing both possible failures—noninjectivity and nonsurjectivity—of the arbitrary tensor/completion comparison map. Positive finite-generation theorems do not satisfy this row. |
| `FC04-C10-U040` | exact Proposition 10.13 | `mathlib` | `AdicCompletion.ofTensorProduct_surjective_of_finite`; `ofTensorProduct_bijective_of_finite_of_isNoetherian`; `ofTensorProductEquivOfFiniteNoetherian` | P | The natural map is surjective for finite modules over any ring and bijective/equivalent for finite modules over a Noetherian ring, exactly Proposition 10.13. |
| `FC04-C10-U041` | exact tensor/completion exactness comparison | `mathlib` | U037/U040 owners; `Mathlib/LinearAlgebra/TensorProduct/RightExactness.lean::LinearMap.lTensor_exact` | P | The finite-module completion equivalences identify the exact completion sequence with tensoring by `Â`; the tensor exactness assembly was compile-checked against P. |
| `FC04-C10-U042` | exact flatness theorem | `mathlib` | `Mathlib/RingTheory/AdicCompletion/AsTensorProduct.lean::AdicCompletion.flat_of_isNoetherian` | P | The adic completion of a Noetherian ring is flat over the original ring, exactly Proposition 10.14. |
| `FC04-C10-U043` | unmatched nonfinite comparison/counterexample | `unmatched` | partial: U040/U042 positive finite-module agreement and flat tensor exactness | N | P/H/open/external search found no source-shaped non-finitely-generated module for which adic completion fails exactness, so the negative comparison row is not formalized as stated. |
| `FC04-C10-U044` | unmatched four-clause Proposition 10.15 bundle | `unmatched` | partial: adic completion tensor equivalence; mapped-ideal/power APIs; `IsAdicComplete.le_jacobson_bot` | N | P supplies clauses corresponding to extension of the ideal, powers, and the Jacobson-radical inclusion, but no exported general isomorphism `a^n/a^(n+1) ≃ â^n/â^(n+1)` was found. Under strict bundle semantics the missing clause (iii) makes the proposition unmatched. |
| `FC04-C10-U045` | exact local-completion theorem | `mathlib` | `Mathlib/RingTheory/AdicCompletion/LocalRing.lean::AdicCompletion.isLocalRing_of_fg`; Noetherian local instance; `AdicCompletion.maximalIdeal_eq_map` | P | For a Noetherian local ring the maximal ideal is finitely generated, the maximal-adic completion is local, and its maximal ideal is the mapped/completed original maximal ideal. |
| `FC04-C10-U046` | exact Krull theorem kernel characterization | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.mem_iInf_smul_pow_eq_bot_iff`; `Mathlib/RingTheory/AdicCompletion/Basic.lean::AdicCompletion.of_apply` | P | The filtration theorem characterizes membership in `⋂ I^n M` by existence of a scalar congruent to one modulo `I` fixing the element; rewriting gives annihilation by an element of `1+I`. Together with the completion map's kernel this is exactly 10.17. The assembly was compile-checked. |
| `FC04-C10-U047` | exact localization-inside-completion bundle | `mathlib` | `IsLocalization.map_eq_zero_iff`; `IsLocalization.lift`; `IsLocalization.lift_injective_iff`; `Mathlib/RingTheory/AdicCompletion/Basic.lean::IsAdicComplete.le_jacobson_bot`; U046 | P | Elements of `1+I` become units in the complete ring, the localization therefore maps to the completion, and the localization kernel criterion plus U046 gives equality of kernels and injectivity. The source-shaped bundle was compile-checked against P. |
| `FC04-C10-U048` | unmatched smooth-function counterexample | `unmatched` | partial: Mathlib smooth glue/flat-function calculus such as `expNegInvGlue.contDiff` | N | No checked Lean development supplies the ring `C^∞(ℝ)`, the maximal ideal at zero, the identification of all ideal powers with vanishing jets, and the `e^{-1/x²}` kernel separation required by the source counterexample. |
| `FC04-C10-U049` | exact Krull intersection corollary | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.iInf_pow_eq_bot_of_isDomain` | P | For a Noetherian domain and proper ideal, the intersection of all powers is zero, exactly Corollary 10.18. |
| `FC04-C10-U050` | exact Jacobson/Hausdorff corollary | `mathlib` | `Mathlib/RingTheory/Filtration.lean::Ideal.iInf_pow_smul_eq_bot_of_le_jacobson`; `Mathlib/RingTheory/AdicCompletion/Noetherian.lean::IsHausdorff.of_le_jacobson` | P | If the ideal lies in the Jacobson radical, the intersection of its powers acting on a finite module is zero; `IsHausdorff.of_le_jacobson` packages the equivalent Hausdorff adic-completion condition. |
| `FC04-C10-U051` | exact local Hausdorff corollary | `mathlib` | `Mathlib/RingTheory/AdicCompletion/Noetherian.lean::IsHausdorff.of_isLocalRing`; maximal-ideal `IsHausdorff` instance | P | Specializing U050 to a Noetherian local ring and its maximal ideal gives Hausdorffness for every finite module, and hence for the ring itself. |
| `FC04-C10-U052` | unmatched p-primary intersection theorem | `unmatched` | partial: primary-ideal APIs; localization kernels; Krull intersection | N | P/H/open/external search found no theorem identifying the intersection of all `p`-primary ideals with the kernel of `A → A_p`. The surrounding localization and primary-decomposition ingredients do not constitute the source corollary. |
| `FC04-C10-U053` | integrated associated graded ring port | `project-existing` | `LeanCategories/Algebra/AssociatedGraded.lean::Ideal.AssociatedGradedRingPiece`; `Ideal.AssociatedGradedRing`; `Ideal.associatedGradedRingMul`; direct-sum `CommRing` instance | L: `f006f0e0a1f0315a0087fc7994a71b0d6672fea8`; PR33218 retained | The source ring `G_a(A)=⊕ n, a^n/a^(n+1)` is realized directly: quotient pieces, representative multiplication with both quotient-descent proofs, graded commutative-ring laws, and the resulting direct-sum commutative ring. The implementation ports/adapts Mathlib PR #33218 (`3c12745297b7236616f6a23d7c11a8addd85e839`, Apache-2.0) to pinned Mathlib rather than substituting the Rees algebra. Representative multiplication and the separating fact that `a^(n+1)` dies in degree `n` are checked in the owner. Focused build and full normal gate passed. |
| `FC04-C10-U054` | integrated associated graded module port | `project-existing` | `LeanCategories/Algebra/AssociatedGraded.lean::Ideal.Filtration.AssociatedGradedModulePiece`; `Ideal.Filtration.AssociatedGradedModule`; `Ideal.Filtration.associatedGradedModuleSMul`; direct-sum module instance over `Ideal.AssociatedGradedRing` | L: `f006f0e0a1f0315a0087fc7994a71b0d6672fea8`; PR33220 retained | For an `I`-filtration `F`, the source module `G(F)=⊕ n, F.N n/F.N(n+1)` is realized with homogeneous action induced by `I^i • F.N j ≤ F.N(i+j)`. Both quotient-descent conditions follow from `F.pow_smul_le`; graded module laws and the direct-sum module structure over the associated graded ring are integrated. This ports/adapts Mathlib PR #33220 (`70572cd62395e933e8f6476bcedcee366a5b5e82`, Apache-2.0) against the pinned API. Representative action and vanishing of the next filtration term are checked. Focused build and full normal gate passed. |
| `FC04-C10-U055` | unmatched three-clause associated-graded proposition | `unmatched` | partial: PR #38331 graded-ring Noetherianity support; PR #33218/#33220 associated-graded structures | N | Current/open work can cover associated-graded Noetherianity, but no checked source supplies the completion graded-ring isomorphism `G_a(A) ≃ G_â(Â)` together with the finite-generation statement for stable filtered modules. The three-clause source bundle therefore remains unmatched. |
| `FC04-C10-U056` | unmatched completion-from-associated-graded criterion | `unmatched` | partial: Mathlib PR #33225/#33227 filtered and associated-graded hom exactness | N | The open filtered/associated-graded map work does not prove the source implication “graded map injective/surjective ⇒ completed map injective/surjective.” No complete project/current/external owner was found. |
| `FC04-C10-U057` | unmatched finite-generation lifting theorem | `unmatched` | partial: completion; associated-graded PRs | N | No checked declaration proves that completeness plus Hausdorffness and finite generation of the associated graded module lift to finite generation of the original filtered module. |
| `FC04-C10-U058` | unmatched Noetherian lifting corollary | `unmatched` | partial: U057 ingredients; Noetherian module APIs | N | No checked declaration lifts Noetherianity of `G(M)` over `G(A)` to Noetherianity of the complete Hausdorff filtered module `M` under the source hypotheses. |
| `FC04-C10-U059` | revision-specific Noetherian adic completion theorem | `reference-port` | Mathlib PR #38331 head `700711ddc105006709aa5e9bb98684001a93859a`, `Mathlib/RingTheory/AdicCompletion/Noetherian.lean::AdicCompletion.isNoetherianRing_of_fg`; general `IsNoetherianRing (AdicCompletion I R)` instance | PR38331 | Open Apache-2.0 Mathlib PR. It proves exactly that the adic completion of a Noetherian ring is Noetherian; the result is absent at P/H and therefore is recorded only at this PR revision. |
| `FC04-C10-U060` | exact formal-power-series Noetherianity | `mathlib` | `Mathlib/RingTheory/MvPowerSeries/Equiv.lean` finite-variable `IsNoetherianRing (MvPowerSeries σ R)` instance; power-series Noetherian infrastructure | P | For finite `σ`, formal power series over a Noetherian coefficient ring are Noetherian; specializing to `Fin n` gives `A[[x_1,…,x_n]]`, and fields are Noetherian. |

